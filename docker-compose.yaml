version: "3.8"
services:
  flask-app:
    image: docker.io/slstef/lab6:latest
    build: /jenkins-pipeline-lab4

  nginx:
    image: nginx:latest
    ports:
      - target: 80
        published: 80
    volumes:
      - type: bind
        source: ./nginx.conf
        target: /etc/nginx/nginx.conf
